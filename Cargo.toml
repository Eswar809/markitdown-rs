[package]
name = "markitdown-rs"
version = "0.1.0"
edition = "2021"
description = "Rust port of microsoft/markitdown — convert documents to Markdown for LLMs"
license = "MIT"

[dependencies]
csv = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
